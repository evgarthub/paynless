import React from 'react';
import { PaymentProps } from './Payment.props';

export const Payment = ({ month, onCreate }: PaymentProps) => {
    
    return (
        <div>
            Payment
        </div>
    )
}